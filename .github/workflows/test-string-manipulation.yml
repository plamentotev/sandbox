name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout
      id: checkout
      uses: actions/checkout@v1
    - name: Add comment.
      id: add_comment
      env:
        GITHUB_JWT: ${{ secrets.GITHUB_TOKEN }}
      run: |
        java -jar maven-spy-1.0-SNAPSHOT.jar ${{ github.sha	}}
